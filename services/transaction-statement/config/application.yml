server:
  port: 5758

spring:
  application:
    name: transaction-statement-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/transaction
    username: postgres
    password: pass
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults: false
